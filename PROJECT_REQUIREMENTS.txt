================================================================================
HUB FINPROMS — PROJECT REQUIREMENTS
================================================================================
Last updated: 2026-09-08
Location: PROJECT_REQUIREMENTS.txt (project root)
IMPORTANT: Refer to this document before and after every code change.
IMPORTANT: Backend and frontend are SEPARATE repos — update BOTH when a
           feature needs UI and API changes.
================================================================================

--------------------------------------------------------------------------------
0. REPOSITORIES / PROJECT LOCATIONS
--------------------------------------------------------------------------------
- Backend:  Desktop/hub-finproms-backend  (this repo)
- Frontend: Desktop/hub-finproms-frontend
- Whenever implementing or changing a feature, apply related changes in the
  frontend as well (UI, API calls, roles, branding, permissions, etc.).
- Do not treat backend-only work as complete if the feature also requires
  frontend updates.


--------------------------------------------------------------------------------
1. PRODUCT OVERVIEW
--------------------------------------------------------------------------------
- Shared hub for FinProms content: social media posts, reels, etc.
- Users subscribe, receive credits, spend credits on posts/reels, then download.
- Same codebase is reused for white-labelled hubs (multi-tenant style).
- Branding (colours, logo, etc.) must come from the database so each hub
  can look different without code changes.


--------------------------------------------------------------------------------
2. SHARED HUB — STEP 1 CORE FEATURES
--------------------------------------------------------------------------------

2.1 Content types (category = type)
- Content category IS the type: e.g. post, reel, etc.
- Types are managed (like categories) and used to classify every item.
- Listing and filters work by type (post / reel / …).

2.2 Content browsing & listing UX
- Users browse posts, reels, and other types.
- For REELS: listing page shows a PLAY button on the card/item.
- "NEW" banner on new posts/reels:
  - Shown when the item is still within the "new" window.
  - Duration (e.g. number of days) is taken from the DATABASE (configurable
    setting), not hard-coded.

2.3 Content metrics
- Each post/reel tracks:
  - views
  - reach
  - buy count
- Metrics are stored and shown (listing and/or detail / admin as appropriate).

2.4 Subscriptions & Credits
- Users subscribe and receive credits.
- Credits are spent when buying posts/reels.
- After purchase, users can download the content.

2.5 One-off / Non-subscriber Purchases
- Users who are NOT subscribed can still buy individual posts/reels.
- Pricing rule: 1 credit = 1 pound (£1).

2.6 Invoices
- Invoices are generated for:
  a) Subscriptions
  b) Individual post/reel purchases

2.7 Roles & client_admin dashboard
- Role rename: former "admin" → "client_admin".
- client_admin has a SEPARATE dashboard from which they can manage everything
  for their hub (posts/reels, types, tags, plans, bank transfers, settings
  such as NEW-banner duration, etc.).
- Regular users use the member-facing UI (browse, subscribe, buy, download).
- power_admin (platform developers) is separate — see section 6.


--------------------------------------------------------------------------------
3. SHARED HUB — FUTURE FEATURES
--------------------------------------------------------------------------------

3.1 AI Content Generation (later)
- Posts/reels added to the hub will also be generated by AI.
- AI will be trained / configured for:
  - Types of posts and reels to generate
  - What text/copy should be added

3.2 In-app Post Editing (later)
- Users who buy a post can edit that post within the application.

3.3 Compliance Check (later)
- After editing (and related flows), posts can go through a compliance check.

3.4 Other Features (later)
- Additional features to be added over time; keep architecture flexible.


--------------------------------------------------------------------------------
4. WHITE-LABELLED HUBS
--------------------------------------------------------------------------------

4.1 Multi-instance / Same Codebase
- The shared hub is copied/reused to form white-labelled hubs.
- There will be MANY white-labelled hubs.
- Code must remain the SAME for shared hub and all white-labelled hubs.
- Everything that differs per hub (especially branding) must be driven by DB:
  - Colour scheme
  - Logo
  - Other tenant-specific settings as needed

4.2 Private Access (White-labelled)
- White-labelled hubs are PRIVATE.
- Public users cannot subscribe.
- Client provides an Excel sheet of advisors.
- Advisors from that sheet are added into the application.
- Those advisors are subscribed and have UNLIMITED credits to buy posts.

4.3 Pricing Tiers for White-labelled (Managed by Shared Hub)
- Pricing depends on number of subscribers (advisors), e.g.:
  - 100 subscribers → £70
  - 150 subscribers → £50
  - (Exact tiers to be configured; managed from shared hub)
- Shared hub manages these pricing rules.

4.4 Content Distribution from Shared Hub → White-labelled
- Shared hub generates posts (including via AI later).
- Shared hub has an option to choose WHICH white-labelled hub(s) receive posts.
- Selected posts are then added/published onto those white-labelled hubs.

4.5 Code Updates
- Updates made in the shared hub codebase are also reflected in white-labelled
  hubs (same codebase / deploy model).


--------------------------------------------------------------------------------
5. RIGHTS / PERMISSIONS CHECKLIST (CRITICAL)
--------------------------------------------------------------------------------
- Because shared and white-labelled use the SAME code, every feature must be
  gated by a rights/permissions checklist.
- Examples of behaviour differences:
  - Subscriptions: open to all users (shared) vs only specific advisors
    (white-labelled).
  - Credits: paid credit packs vs unlimited for advisors.
  - Public subscribe vs private invite-only.
- Checklist controls which functionality is ON/OFF or HOW it works per hub.
- Each white-labelled hub can have its own checklist configuration.


--------------------------------------------------------------------------------
6. POWER ADMIN (SHARED HUB)
--------------------------------------------------------------------------------
- Role: power_admin (platform developers / us).
- Dashboard exists on the SHARED hub.
- From this dashboard, power_admin can manage the checklist
  (permissions, rights, functionality) for EACH white-labelled hub.
- This is the central control plane for tenant behaviour.
- Distinct from client_admin (hub operator who manages content/plans/etc.).


--------------------------------------------------------------------------------
7. ARCHITECTURE PRINCIPLES (DERIVED FROM REQUIREMENTS)
--------------------------------------------------------------------------------
- Single codebase for shared + all white-labelled hubs.
- Tenant/hub configuration stored in database (branding, flags, rights).
- Feature flags / rights checklist must drive behaviour, not hard-coded forks.
- Shared hub acts as content + configuration source of truth for white labels.
- Design for future: AI generation, in-app edit, compliance, more features.
- Settings that vary (e.g. NEW-banner duration) come from the database.


--------------------------------------------------------------------------------
8. IMPLEMENTATION STATUS NOTES
--------------------------------------------------------------------------------
(Update this section as features are built.)

STEP 1 — Shared hub core
[x] Shared hub content browsing (posts) — basic exists
[x] Content type = category (post, reel, etc.)
[x] Reel play button on listing page
[x] NEW banner (duration from database)
[x] Views, reach, buy count on content
[x] Role rename: admin → client_admin
[x] Separate client_admin dashboard (manage everything)
[x] Subscriptions + credits (basic exists)
[x] Credit spend + download (basic exists)
[ ] Non-subscriber one-off purchase (1 credit = £1)
[ ] Invoices for subscriptions and purchases

LATER / WHITE-LABEL
[ ] Tenant/hub DB-driven branding (colours, logo)
[ ] Rights/permissions checklist model
[ ] power_admin dashboard to manage per-hub checklist
[ ] White-labelled private advisor import (Excel)
[ ] Unlimited credits for white-labelled advisors
[ ] Subscriber-count pricing tiers (managed by shared hub)
[ ] Push/select posts from shared hub to white-labelled hubs
[ ] AI post/reel generation (later)
[ ] In-app post editing for buyers (later)
[ ] Compliance check flow (later)

================================================================================
END OF REQUIREMENTS
================================================================================
